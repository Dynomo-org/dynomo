version: "3.9"
services:
  app:
    image: "ibrahimsyah/dynapgen:latest"
    environment:
      ENV: production
      GITHUB_CLIENT_KEY: ${GITHUB_CLIENT_KEY}
    ports:
      - "3002:5000"
    volumes:
      - ./:/usr/src/app/
  redis:
    image: "redis:alpine"